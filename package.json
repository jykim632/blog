{
  "name": "yeobaek-records",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "packageManager": "pnpm@10.11.1",
  "scripts": {
    "dev": "pnpm build && pnpm db:migrate:local && wrangler dev --config dist/server/wrangler.json --env-file .dev.vars --persist-to .wrangler/state --port 4321",
    "build:develop": "CLOUDFLARE_ENV=develop astro check && CLOUDFLARE_ENV=develop astro build && node scripts/prepare-develop-worker-config.mjs",
    "build": "astro check && astro build",
    "preview": "astro preview",
    "cf:dev": "wrangler dev --config dist/server/wrangler.json --env-file .dev.vars --persist-to .wrangler/state",
    "deploy": "pnpm build && wrangler deploy --config dist/server/wrangler.json",
    "deploy:develop": "pnpm run build:develop && wrangler deploy --config dist/server/wrangler.json",
    "db:migrate:local": "wrangler d1 migrations apply blog --local",
    "db:migrate:develop": "wrangler d1 migrations apply blog-develop --remote --env develop",
    "db:migrate:remote": "wrangler d1 migrations apply blog --remote",
    "db:import:legacy": "node scripts/import-legacy-posts.mjs"
  },
  "dependencies": {
    "@astrojs/cloudflare": "^14.1.3",
    "@tiptap/core": "^3.28.0",
    "@tiptap/starter-kit": "^3.28.0",
    "astro": "^7.1.1",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.9",
    "typescript": "^5.8.0",
    "wrangler": "^4.25.0"
  }
}
