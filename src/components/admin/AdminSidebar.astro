---
import SiteHeader from '../SiteHeader.astro';

interface Props { active: 'posts' | 'media'; }
const { active } = Astro.props;
---

<SiteHeader variant="admin" activeAdmin={active} />

<style is:global>
  .admin-shell{min-height:100vh;background:#fff}
</style>
